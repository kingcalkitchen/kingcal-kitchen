import { StyleSheet } from 'react-native'
import { FormField } from '../../AppStyles'

const styles = StyleSheet.create({
    container: FormField.container,
    textInput: FormField.textInput,
})

export default styles
